<h1>Doctor <%= @doctor.name %></h1>
<p>
  <h2>Appointments</h2>
  <ul>
    <% @doctor.appointments.each do |appointment| %>
      <li><%= appointment.human_readable_date %> (<%= link_to appointment.patient.name, appointment.patient %>)</li>
    <% end %>
  </ul>
</p>
